# 시연 스크립트 — PDF 발송 시스템 (#154865)

> 1분 30초 · 5단계 데모. 실제 미팅에서 화면 공유하며 따라 읽기.

---

## 0:00–0:08 ‖ 인트로 (대시보드)

> "안녕하세요. 위시켓 154865 PDF 발송 시스템 프로토타입입니다.
> 실제 데이터가 연결된 것처럼 보이지만 모두 데모 데이터고요,
> 4개 화면으로 구성된 관리자 콘솔을 차례로 보여드리겠습니다."

**화면**: Dashboard 첫 진입
**포인트**:
- 좌측 사이드바 4개 메뉴 (대시보드·발송·이력·템플릿)
- 상단 4 KPI · 7일 추이 차트 · 채널 비중 도넛
- "데모 데이터" 우하단 노란 배지 강조

---

## 0:08–0:35 ‖ 발송 콘솔 4스텝 (핵심)

> "발송 콘솔로 가보겠습니다. 가장 자주 쓰실 화면이에요.
> 4단계 위저드로 구성했고 각 단계는 도메인 모델의 책임이 1:1로 매핑됩니다."

### 0:08 Step 1 — 대상 조회
> "Oracle DB에서 미발송 고지서를 조회합니다.
> 좌하단에 Oracle 연결 상태가 녹색 점으로 표시되고요,
> 체크박스로 다중 선택, 우측 상단에 선택 건수가 실시간 누적됩니다."

### 0:18 Step 2 — 템플릿 선택
> "템플릿을 고르면 우측에 PDF 미리보기가 A4 비율로 즉시 렌더링됩니다.
> 이 미리보기는 Tweaks의 브랜드 색을 라이브로 따라갑니다."

### 0:25 Step 3 — 채널 정책
> "여기가 핵심입니다.
> 우측 상단 Tweaks에서 PRIORITY를 BROADCAST로 바꾸면
> Step 3 동작이 실제로 달라져요 — 우선순위 1/2/3 번호가 사라지고 동시 발송으로 전환됩니다.
> 정책 엔진이 단순 표시가 아니라 실제로 동작한다는 점을 보여드리는 부분입니다."

### 0:30 Step 4 — 진행률
> "발송 실행하면 큐 기반 비동기 진행률이 0%부터 100%까지 표시되고,
> 완료 즉시 결과 카드에서 성공·실패 수가 분리되어 나옵니다."

---

## 0:35–0:55 ‖ 이력·재발송

> "이력 화면에서는 모든 발송 레코드를 검색·필터링할 수 있습니다."

**클릭 흐름**:
1. 상단 필터: 상태=실패, 채널=웹팩스 → 표시 건수 변화
2. 박민수 행 클릭 → 우측 상세 패널 슬라이드인
3. 패널 하단 "이메일로 재발송" 버튼 → 채널 변경 후 재시도

> "여기서 박민수 고객을 보시면, 첫 시도는 팩스로 실패했지만
> 정책 엔진이 자동으로 이메일로 fallback해서 두 번째 시도는 성공했습니다.
> 이게 ENGINE의 자동대체 정책이 실제 데이터에 반영된 모습입니다."

---

## 0:55–1:15 ‖ 템플릿 관리

> "마지막은 템플릿 편집기입니다."

**클릭 흐름**:
1. 좌측 목록에서 "전기료 고지서" 선택
2. 중앙 편집 영역에서 brandColor를 #3aeec1로 변경
3. **우측 PDF 프리뷰가 즉시 반영** ← 이 한 컷이 핵심

> "디바운스 없이 즉시 반영되는 게 포인트입니다.
> 프로덕션에서는 PDF 렌더링 솔루션을 통해 동일하게 동작하도록 구현하겠습니다."

---

## 1:15–1:30 ‖ 마무리

> "지금 보신 4개 화면은 위시켓 산출물 중 '관리자 화면 UI'에 해당하고요,
> 이 정도 디자인 깊이로 14주 일정 안에 완성하겠습니다.
> 30분 미팅에서 더 자세히 설명드리고 싶습니다. 감사합니다."

**마지막 화면**: Dashboard로 복귀 → 사이드바 + 데모 배지 + 회사 로고

---

## 녹화 가이드 (macOS)

1. 프로토타입을 1280×720 창 크기로 띄우기 (브라우저 zoom 100%)
2. ⌘ Cmd+Shift+5 → "선택 부분 기록" → 프로토타입 영역 드래그
3. "녹화" 버튼 클릭 후 위 스크립트 따라 클릭
4. 녹화 중지 후 mov 파일을 ffmpeg로 mp4 변환 (선택):
   ```
   ffmpeg -i demo.mov -vcodec h264 -acodec aac demo.mp4
   ```
5. Loom 무료 플랜 대신 자체 mp4 사용 가능

## 음성 더빙 (선택)

- 위 스크립트를 텍스트→음성 변환 (macOS `say` / OpenAI TTS / ElevenLabs)
- iMovie에서 화면 녹화 위에 음성 트랙 추가
- 책정 시간: 1시간 이내
